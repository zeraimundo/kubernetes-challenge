apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: appjr-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: appjr-service
                port:
                  number: 4000